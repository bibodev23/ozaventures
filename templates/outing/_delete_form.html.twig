<form method="post" action="{{ path('app_outing_delete', {'id': outing.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ outing.id) }}">
    <button class="btn">Delete</button>
</form>
