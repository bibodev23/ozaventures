{% extends 'base.html.twig' %}

{% block title %}Sortie {{outing.title}}{% endblock %}
    {% block classPage %}outing-edit{% endblock %}
{% block header %}
	<h1>Sortie {{outing.title}}</h1>
        <a href="{{path('app_kid_new')}}" class="btn green">Nouvelle fiche</a>
{% endblock %}
{% block body %}
    {{ include('outing/_form.html.twig', {'button_label': 'Enregistrer'}) }}
 <div class="list-actions">
    <a href="{{ path('app_outing_index') }}" class="btn">Retour à la liste</a>
    {{ include('outing/_delete_form.html.twig') }}
        </div>
{% endblock %}
