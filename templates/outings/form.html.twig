{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <section class="page-heading">
        <div>
            <p class="eyebrow">Sorties</p>
            <h1>{{ title }}</h1>
            <p class="lead lead-compact">Prépare une sortie claire pour la direction, les affiches et le suivi de saison.</p>
        </div>
    </section>

    <section class="form-layout">
        <aside class="side-panel outing-checklist-panel">
            <div class="checklist-heading">
                <p class="panel-kicker">Checklist</p>
                <span>5 points</span>
            </div>
            <h2>Avant de valider la sortie</h2>
            <p class="checklist-intro">Les points clés à verrouiller pour une sortie lisible et sécurisée.</p>
            <ol class="check-list check-list-prominent">
                <li>
                    <strong>Sac pharmacie</strong>
                    <span>À prévoir systématiquement pour la sortie.</span>
                </li>
                <li>
                    <strong>Horaires</strong>
                    <span>Vérifie le départ et le retour avant validation.</span>
                </li>
                <li>
                    <strong>Enfants</strong>
                    <span>Sélectionne tous les participants concernés.</span>
                </li>
                <li>
                    <strong>Encadrement</strong>
                    <span>Associe au moins un animateur responsable.</span>
                </li>
                <li>
                    <strong>Décision</strong>
                    <span>Garde la sortie en attente tant que les informations ne sont pas complètes.</span>
                </li>
            </ol>
        </aside>

        <section class="form-panel form-panel-wide">
            {{ form_start(form, {attr: {class: 'form'}}) }}
                <section class="hint-card">
                    <strong>Préparation direction</strong>
                    <span>La sortie sera enregistrée pour validation, suivi des participants et impression de l'affiche.</span>
                </section>
                <div class="form-grid">
                    {{ form_row(form.number) }}
                    {{ form_row(form.destination) }}
                    {{ form_row(form.departureAt) }}
                    {{ form_row(form.returnAt) }}
                    {{ form_row(form.transportMode) }}
                    {{ form_row(form.picnicRequired) }}
                    {{ form_row(form.routeDurationMinutes) }}
                </div>
                <div class="selection-grid">
                    <div class="selection-box outing-selection-box">
                        {{ form_row(form.children) }}
                    </div>
                    <div class="selection-box outing-selection-box">
                        {{ form_row(form.animators) }}
                    </div>
                </div>
                <div class="form-actions">
                    <a class="button button-secondary" href="{{ outing.id ? path('app_outing_show', {id: outing.id}) : path('app_outings') }}">Annuler</a>
                    <button class="button button-primary" type="submit">Enregistrer</button>
                </div>
            {{ form_end(form) }}
        </section>
    </section>
{% endblock %}
